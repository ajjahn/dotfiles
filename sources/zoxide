# vi:ft=bash:

eval "$(zoxide init zsh)"

alias cd="zd"
zd() {
  if [ $# -eq 0 ]; then
    builtin cd ~ && return
  elif [ -d "$1" ]; then
    builtin cd "$1" && return
  else
    z "$@" && printf " \U000F17A9 " && pwd || echo "cd: no such file or directory:" "$@"
  fi
}
